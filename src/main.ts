import { predictSalesAmount } from './functions';

// The game name you want to predict the sales for
const gameName = 'Super Shenkar Bros';

// Call the function to predict the sales and log it to the console
predictSalesAmount(gameName);